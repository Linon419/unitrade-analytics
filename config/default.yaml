# ╔════════════════════════════════════════════════════════════════════════════╗
# ║                    UniTrade Analytics Gateway 配置                          ║
# ╚════════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 交易所配置                                                                    │
# └──────────────────────────────────────────────────────────────────────────────┘
exchanges:
  binance:
    enabled: true
    api_mode: "public"           # public / private
    api_key: ""                  # 或环境变量 BINANCE_API_KEY
    api_secret: ""               # 或环境变量 BINANCE_API_SECRET
    testnet: false
    max_connections: 5
    heartbeat_interval: 30
    reconnect_max_attempts: 10
    symbols: [BTCUSDT, ETHUSDT]

  bybit:
    enabled: false
    api_mode: "public"
    api_key: ""
    api_secret: ""
    testnet: false
    heartbeat_interval: 20
    reconnect_max_attempts: 10
    symbols: [BTCUSDT, ETHUSDT]

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 数据存储配置                                                                  │
# └──────────────────────────────────────────────────────────────────────────────┘
database:
  sqlite_path: "data/unitrade.db"
  redis_url: "redis://localhost:6379"
  data_retention_days: 30

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ Telegram 机器人配置 (全局)                                                    │
# └──────────────────────────────────────────────────────────────────────────────┘
telegram:
  enabled: true
  # 建议把敏感信息放到环境变量（不要提交到 git）:
  #   TELEGRAM_BOT_TOKEN=...
  #   TELEGRAM_CHAT_ID=...
  bot_token: ""
  chat_id: ""
  
  # 话题频道 ID 映射 (message_thread_id)
  topics:
    general: 1                        # 通用
    ema_ranking: 2                    # EMA最强/最弱
    ema_entering: 3                   # EMA刚进入强势/弱势
    ema_flowering: 4                  # EMA开花 🌸
    squeeze_daily_weekly: 5           # 日线/周线波动预警
    squeeze_4h_1h: 6                  # 4h/1h波动预警
    divergence_normal: 7              # 普通背离
    divergence_volume: 8              # 放量背离
    wavetrend_overbought_oversold: 9  # 波段过滤器超买/超卖
    signal_anomaly: 62                # 异动信号推送 🚀
  
  # 定时推送开关
  scheduled_reports:
    ema_report: false                 # 定时推送 EMA 雷达报告
    market_report: false              # 定时推送 BTC 综合数据报告
    ema_interval: 3600                # EMA 报告间隔 (秒)
    market_interval: 1800             # 市场报告间隔 (秒)

# ╔════════════════════════════════════════════════════════════════════════════╗
# ║                           扫描器模块配置                                      ║
# ╚════════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 1. 异动检测器 (EMA200突破 + OI异动 + 放量) → anomaly_detector.py              │
# └──────────────────────────────────────────────────────────────────────────────┘
anomaly_detector:
  enabled: true
  
  timeframes: ["15m", "30m"]
  
  # 触发条件
  oi_change_threshold: 0.03      # OI 增加 ≥ 3%
  oi_lookback_bars: 3            # 对比 N 根 K 线前的 OI
  ema_period: 200                # EMA200 突破
  rvol_threshold: 3.0            # 放量倍数 ≥ 3x
  rvol_lookback: 20              # 计算平均量的 K 线数
  cooldown_seconds: 1800         # 冷却 30 分钟
  
  # 币种筛选
  scan_all: true
  top_n: 50
  exclude_symbols: []
  kline_limit: 500
  
  # Rising Index 排行
  rising_index:
    enabled: true
    price_structure_weight: 0.35
    oi_flow_weight: 0.30
    recency_weight: 0.20
    volume_weight: 0.15
    recency_decay_hours: 24.0
    signal_ttl_days: 5
  
  # Telegram 定时推送
  telegram_push:
    enabled: true
    interval_hours: 4
    top_n: 10

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 2. 波动预警扫描器 (BB/KC 挤压) → squeeze_momentum_scanner.py                  │
# └──────────────────────────────────────────────────────────────────────────────┘
squeeze_scanner:
  enabled: true
  
  timeframes: ["1h", "4h", "1d"]
  
  # BB/KC 参数
  squeeze_len: 20
  bb_mult: 2.0
  kc_mult: 1.5
  atr_len: 20
  
  # 触发条件
  min_squeeze_duration: 5
  tightness_l2_ratio: 0.80
  momentum_len: 12
  
  # 币种筛选
  top_n: 50
  extra_whitelist: [BTCUSDT, ETHUSDT]
  kline_limit: 200
  max_concurrent: 20

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 3. EMA 趋势雷达 → ema_radar.py                                               │
# └──────────────────────────────────────────────────────────────────────────────┘
ema_radar:
  enabled: false
  
  timeframes: ["1h", "4h"]
  ema_periods: [21, 55, 100, 200]
  near_ema_threshold: 0.01
  top_n_results: 10

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 4. 波段过滤器 (WaveTrend) → wavetrend_scanner.py                             │
# └──────────────────────────────────────────────────────────────────────────────┘
wavetrend_scanner:
  enabled: false
  
  timeframes: ["5m", "15m", "30m", "1h", "4h", "1d"]
  algo_type: "WaveTrend + MFI Hybrid"
  use_ha: false
  
  # WaveTrend 参数
  channel_len: 9
  avg_len: 7
  smooth_len: 4
  ma_type: "ALMA"
  laguerre_gamma: 0.66
  
  # MFI 混合
  wt_weight: 0.4
  mfi_len: 7
  hybrid_mfi_len: 7
  
  # 阈值
  lvl_1: 45
  lvl_2_base: 60
  lvl_2_floor: 50
  lvl_3_base: 68
  lvl_3_floor: 48
  mult: 1.2
  step_size: 6.6
  sigmoid_gain: 1.8
  use_sigmoid: true
  osc_min: -60
  osc_max: 60
  
  # 成交量
  vol_len: 60
  vol_trigger: 1.6
  vol_weight: 3.0
  
  # 波动预警 (内置)
  squeeze_len: 20
  bb_mult: 2.0
  kc_mult: 1.5
  atr_len: 20
  min_squeeze_duration: 5
  tightness_l2_ratio: 0.80
  
  # 背离检测
  lb_left: 20
  lb_right: 0
  search_limit: 65
  require_candle_conf: true
  use_adx_filter_div: false
  adx_len: 14
  adx_limit: 30
  ha_warmup: 0
  kline_limit: 500
  use_kline_cache: true
  kline_db_path: "data/klines.db"
  
  # 性能
  top_n: 50
  max_concurrent: 20
  extra_whitelist: [BTCUSDT, ETHUSDT, SOLUSDT, DOGEUSDT, XRPUSDT]

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 5. OI 飙升检测器 (持仓量突变) → squeeze_scanner.py                            │
# └──────────────────────────────────────────────────────────────────────────────┘
oi_spike_scanner:
  enabled: false
  
  auto_top_n: 150
  extra_whitelist: [PEPEUSDT]
  ignore_list: [BTCUSDT, ETHUSDT, BNBUSDT]
  
  # OI 分析
  oi_period: "5m"
  oi_limit: 30
  ma_short_periods: 3
  ma_long_periods: 30
  spike_threshold: 1.20
  
  # 并发
  max_concurrent_requests: 10
  request_delay: 0.1
  scan_interval_minutes: 5

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 6. 实时异动信号检测 → signal_detector (内置)                                  │
# └──────────────────────────────────────────────────────────────────────────────┘
signal_detector:
  enabled: false 
  
  rvol_threshold: 1.5
  net_flow_threshold: 80000
  price_change_threshold: 2.0
  rebound_threshold: 5.0
  debounce_minutes: 5
  min_quote_volume_24h: 1000000
  max_symbols: 50
  topic_id: 62

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 7. 实时数据服务 → realtime_service.py                                        │
# └──────────────────────────────────────────────────────────────────────────────┘
realtime:
  enabled: true
  symbols:
    - BTCUSDT
    - ETHUSDT
    - BNBUSDT
    - SOLUSDT
    - DOGEUSDT
    - LTCUSDT
    - LDOUSDT
    - CRVUSDT
    - LINKUSDT
    - ADAUSDT
    - UNIUSDT
    - ONDOUSDT
    - AAVEUSDT
    - AVAXUSDT
    - PEPEUSDT
    - SUIUSDT
    - SEIUSDT
    - WLDUSDT
    - HYPEUSDT
    - TRUMPUSDT
    - PUMPUSDT
    - ZECUSDT
  max_buckets: 100
  db_path: "data/market_data.db"

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 分析引擎配置                                                                  │
# └──────────────────────────────────────────────────────────────────────────────┘
analytics:
  obi_depth_levels: 10
  cvd_history_size: 1000
  volatility_window_seconds: 60
  volatility_sample_interval: 1.0
  oi_poll_interval: 5.0
  oi_history_size: 60
  oi_change_threshold: 0.005
  price_change_threshold: 0.001

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ 系统配置                                                                      │
# └──────────────────────────────────────────────────────────────────────────────┘
log_level: INFO
